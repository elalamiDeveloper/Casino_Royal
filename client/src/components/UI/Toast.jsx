import { ToastContainer } from 'react-toastify';

const Toast = () => {
  const styles = { fontSize: '1.6rem', fontWeight: '600' };

  return <ToastContainer style={styles} />;
};

export default Toast;
